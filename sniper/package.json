{
  "name": "pumpfun-sniper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "verify": "tsx src/verify.ts",
    "verify:amm": "tsx src/verify-swap.ts",
    "test:exits": "tsx src/selftest-exits.ts",
    "test:filters": "tsx src/selftest-filters.ts",
    "test:crowd": "tsx src/selftest-crowd.ts",
    "test:selection": "tsx src/selftest-selection.ts",
    "test:routing": "tsx src/selftest-routing.ts",
    "test": "npm run -s test:filters && npm run -s test:exits && npm run -s test:crowd && npm run -s test:selection && npm run -s test:routing && npm run -s test:memory && npm run -s test:fills",
    "replay": "tsx src/replay.ts",
    "replay:entry": "tsx src/replay-entry.ts",
    "replay:split": "tsx src/replay-split.ts",
    "replay:regime": "tsx src/replay-regime.ts",
    "replay:signals": "tsx src/replay-signals.ts",
    "replay:activity": "tsx src/replay-activity.ts",
    "replay:momentum": "tsx src/replay-momentum.ts",
    "typecheck": "tsc --noEmit",
    "test:memory": "tsx src/selftest-memory.ts",
    "test:fills": "tsx src/selftest-fills.ts"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.0",
    "bs58": "^6.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
